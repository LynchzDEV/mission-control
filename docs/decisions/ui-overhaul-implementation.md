# Implement the approved Mission Control design

The user approved `visualizer/ui-overhaul/terminal-components.html` and now explicitly requests real implementation compatible with existing functions. This is the current implementation brief. It supersedes ui-overhaul-brief.md and ui-overhaul-correction.md visually; do not repeat design research or use their rejected mineral/light composition.

## Visual reference and result

Read the approved HTML and copy its actual component structure/tokens into the real views and client renderers. At 1512×909 the masthead has compact navigation and three aligned provider summaries; the original grey ASCII wave remains above the workspace; flow and a live sub-agent mini window sit above two open terminal surfaces; sessions sit below. Keep the lime/coral/blue identity, colored session emblems, thin divisions, native meters, subtle flow signals, and reduced-motion handling. This must look like the approved mockup, not the previous app with a dark recolor. Real terminal output is xterm, never the sample HTML transcript or a replacement fake command field.

## Scope and acceptance

1. Apply the approved identity to shell, Terminals, Overview/flow, Usage, Dispatch, Review, Settings, login/setup, and drawers. Reuse the already recomposed secondary work screens where sound; fix their current defects rather than returning legacy tables/racks. Every existing direct route and navigation action stays reachable. Terminal surfaces/sockets survive navigation. No permanent directory sidebar absent from the approved layout; directory/resume controls can open a contextual surface.
2. Wire terminal controls to existing actions: create/model/cwd, resume supported history, input/copy/paste/uploads, multiple sessions, side-by-side and stacked splits, pointer/keyboard resizing, per-pane focus/restore, search, rename, close a split without killing the session, explicit terminate, reconnect and stale-socket guards. Do not auto-create sessions from a sample. Preserve `mc:terminal-scope` and actual per-session DOM ownership. Zero/one/many/ended sessions and unavailable tools get usable feedback; no sample jobs, fake metadata, or simulated success in real views.
3. Flow is prominent and real: use actual flow/manual plan and job/thread relationships, with selection tied to the current terminal/work where possible. Reuse parsers from plan-view/thread-view; do not imply a manual plan is an automatic scheduler, or duplicate an unrelated label's latest activity across older jobs. Show plan-only/no-plan/empty/error states. The mini agent window uses actual scoped job thread/activity via createMiniFeed/related helpers, can select/open a full conversation/reply, and handles multiple, no active, recent, completed, failed and waiting agents. Preserve full agent management actions in an accessible expanded view.
4. Implement the consistent 5h/weekly provider summaries from live quota data with native meters and clear labels. CURRENT contract: Claude available blockPercent is a nullable ccusage 5h ESTIMATE with nullable resetsAt; GLM fiveHourPct is available and monthlyPct is MONTHLY; Codex presently only available/authed. Weekly values and Codex usage are absent today. Keep missing meters/percentages explicitly unavailable, never zero and never a converted monthly value. Keep actual reported costs/tokens/monthly detail reachable in Usage. Parent is investigating supported missing sources; do not build speculative provider collectors in this frontend job. A small pure normalization helper with tests is appropriate. Prepare for additive optional fiveHourPct/weeklyPct plus fiveHourResetsAt/weeklyResetsAt fields on provider success records; use those actual fields when present, with existing Claude blockPercent only as a labelled estimate fallback. Parent will add supported Codex limit collection separately.
5. Preserve new-job creation/draft/model/reset behavior, error/pending feedback, thread replies and polling focus/caret/scroll, reviewable earlier thread members and correct member-targeted reviewed/land actions. Prior repair partially landed before design pause: do not assume it passed. Root browser previously reproduced cached New job not reopening, reply textarea replacement on metadata polling, older root changes hidden by a no-diff reply, and terminal double-click rename target replacement. Verify/fix if still present. Keep landing separate from acknowledgement and preserve confirmation/error details.
6. Build the actual templates using existing scripts/render-ui-preview.ts so parent can inspect before activation; paths/assets/CSS must load without interception. Preview Settings writes stay disabled and visibly labelled. Inform parent as soon as real shell/Terminals/flow/agent area is ready. Parent owns Arc/browser, exact screenshot comparisons and safe backend-persistence verification.

## Ownership and limits

Own `server/views/*.tsx`, `client/*.ts`, `public/theme.css`, `public/theme-tokens.css`, `design/theme-tokens.css`, relevant tests, and `scripts/render-ui-preview.ts`. Small `server/index.ts` changes only if needed for frontend rendering/caching. Reuse installed Bun/Elysia/xterm/anime helpers; no new framework or dependency. Backend job, PTY, authentication, write-action and config semantics remain intact. Quota collector extensions, if justified, are a separately scoped step after this frontend pass.

Work in the current main checkout. No new branch, worktree, commit, push, service restart, process kill, real agent/PTY executions for tests, or external messages. Do not modify the approved visualizer, its decision, AGENTS.md, unrelated worktrees, credentials, or this brief. You are not alone in the tree: preserve others' changes. Existing broad app modifications are the unfinished prior attempt; integrate and repair them without resetting the tree. No more design planning or gallery research.

While iterating run only the tests for files you touch; run the full suite ONCE at the end. Preserve behavior/security assertions; update obsolete appearance assertions only for deliberate new rendering. Build every changed browser entrypoint. Leave focused runnable checks for new quota normalization, flow selection and actual changed lifecycle branches. Report exact pass/fail counts, changed files, remaining functional limitations and preview readiness; parent verifies the final render.

Comments: max 1 line, only for a trap no refactor can express. Decision records / measurements / rationale → commit body or docs/decisions/*.md — NEVER comment blocks.
Do NOT match the surrounding code's comment density — existing dense files are legacy, not license.
