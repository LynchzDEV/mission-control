# Connect the approved usage summaries

Follow-on to ui-overhaul-implementation.md. The approved masthead uses consistent 5-hour usage with a smaller weekly value for each provider. Add only data sources verified to exist; missing windows must remain unavailable.

## Ownership and acceptance

- Own server/quota.ts, its focused tests, and at most one small transport helper if useful. Coordinate any necessary client/quota display edits with the parent after reading its final frontend contract. Preserve the completed frontend composition, all existing costs/tokens/auth fields, GLM monthly reporting and the existing 60-second route cache.
- Codex: use the installed CLI's supported `codex app-server --stdio` JSON-RPC `account/rateLimits/read`. This was tested against the local account without creating a thread/turn; it currently returns a weekly window only. Initialize with clientInfo and experimentalApi capability, wait for its matching response, send initialized notification, then the rate-limit request. Read only matching IDs; tolerate unrelated notifications and split stdout chunks. Prefer rateLimitsByLimitId.codex, otherwise the legacy rateLimits bucket when appropriate. Never substitute a different bucket or infer a missing window. Match windowDurationMins 300 to fiveHourPct and 10080 to weeklyPct regardless of primary/secondary position. usedPercent must be finite and in 0..100; resetsAt is Unix seconds, normalize to ISO strings. Preserve login status separately from usage availability.
- Bound each collection, including startup and EOF/error handling, to a short timeout. Consume/discard stderr; never expose raw CLI errors/account responses or credentials. Close stdin and dispose only the child spawned by this collector, even on errors/timeouts. Reuse existing command/validation patterns where possible; do not build a generic RPC framework or add dependencies. Avoid spawning a paid model turn or calling an undocumented endpoint.
- Claude: optionally read the ALREADY-EXISTING ccstatusline usage cache at ~/.cache/ccstatusline/usage.json. Source fields are sessionUsage, weeklyUsage, sessionResetAt, weeklyResetAt. Accept only a fresh file (180 seconds, reject future timestamps), finite percentages in 0..100, valid reset strings; show source as cached with its observation time. Missing, stale, malformed or unreadable cache is normal: retain the current labelled ccusage estimate fallback and no fake weekly value. Do not run ccstatusline, refresh via an undocumented endpoint, capture credentials, or edit global statusline/config files. Keep existing fetchClaudeQuota injection tests independent of the actual user's cache.
- Extend success records additively with optional nullable fiveHourPct, weeklyPct, fiveHourResetsAt, weeklyResetsAt. Include source/observedAt/reason only when needed to distinguish cached data and unavailable limits. Do not turn quota-source failure into a false authentication failure. GLM has no verified weekly window: do not relabel monthly or extrapolate. Keep unavailable values distinct from actual zero.
- Add focused runnable checks covering reversed/missing windows (especially weekly-only), unrelated buckets, malformed percentages/reset timestamps, RPC errors/EOF/timeout/cleanup, fresh versus stale/missing Claude cache, and preservation of existing cost/token/auth output. Inject transport/files/time to keep tests deterministic and prevent tests launching the real CLI or reading user config. Parent will run one safe live read of the resulting collector and browser check.

## Evidence

Installed Codex CLI 0.153.4 and a local read returned a 10080-minute window with usedPercent 9; there was no 300-minute window. Protocol: https://developers.openai.com/codex/app-server . Claude 2.1.263 supports official statusline rate_limits.five_hour and rate_limits.seven_day inputs, but this app does not capture them; do not add new global plumbing in this change. Existing cache was independently inspected with only the four allowlisted fields and mtime. Its producer uses a 180-second freshness ceiling.

Work in main, no branch/worktree/commit/push/service restart or existing process kills. You are not alone in the codebase; preserve other edits. This is a serial follow-on after the frontend job, not a parallel writer. While iterating run only the tests for files you touch; run the full suite ONCE at the end. Report exact checks and any unavailable provider windows.

Comments: max 1 line, only for a trap no refactor can express. Decision records / measurements / rationale → commit body or docs/decisions/*.md — NEVER comment blocks.
Do NOT match the surrounding code's comment density — existing dense files are legacy, not license.
