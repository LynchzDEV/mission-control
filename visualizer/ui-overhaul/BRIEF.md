# Mission Control: UI overhaul preview

User request: overhaul the entire UI/UX, geeky but clean and minimalist, no AI slop; approach them with a new design using visualize-me and show as much as possible. This is a design decision artifact, not approved production implementation.

## Ownership and boundaries

You are a Mission Control worker job: implement directly in this tree, do NOT dispatch, do NOT post plans, do NOT invoke mc-dispatch.
You own only visualizer/ui-overhaul/index.html, app.js, styles.css, tokens.css, fingerprint.js, optional locally bundled fonts/, and a README.md inside this directory.
The orchestrator owns serve.ts, BRIEF.md, current/ snapshots, evidence/, and verification scripts. Do not edit these.
You are not alone in the codebase. Do not revert edits made by others; accommodate the orchestrator's files. Do not edit app source, root configuration, git index, AGENTS.md, or any other path. No commits, branches, worktrees, dependency installs, service restarts, network API calls, storage, or real engine dispatches. Preview is already served on http://127.0.0.1:47831. No API requests from the preview. Hardcoded fixtures and cosmetic in-memory state only.
Comments: max 1 line, only for a trap no refactor can express. Decision records / measurements / rationale → commit body or docs/decisions/*.md — NEVER comment blocks.
Do NOT match the surrounding code's comment density — existing dense files are legacy, not license.

## Reference reading

Read server/views/{layout,terminals,lanes,dispatch,review,settings}.tsx and public/theme-tokens.css to preserve actual workflows and names. Current navigation names: Lanes, Dispatch, Terminals, Review, Settings. Engines Claude, GLM, Codex. Roles Plan, Execute, Review, optional model, auto-review off by default. Existing terminal has sessions, resume, agent sidebar, new terminal form. Dispatch has engine/model/repo/label/prompt/worktree. Review has job/diff/details/mark reviewed, thread continuation. Lanes has session flow, plan steps, activity, quota by engine. Settings has engines, per-role engine/model assignments, auto review, connection state, masked credentials. Search existing implementation before inventing equivalence. Preserve names, simplify displayed wording where appropriate.

Apply frontend-design skill (/Users/lynchz/.codex/plugins/cache/claude-plugins-official/frontend-design/local/skills/frontend-design/SKILL.md) and Hallmark principles (/Users/lynchz/.agents/skills/hallmark/SKILL.md). User explicitly asks for a new visual language, overriding visualize-me's usual locked styling. This is a working software interface, not a marketing page; no hero, mascots, decorative bento metrics, fake browser chrome, gradients, glows, pixel font, AI sparkle icons, or excessive badges. Terminal/tool content is real product UI, not decorative imitation. Use semantic HTML, small coherent outline icons, elegant type, fine rules, whitespace and strong alignment. Never sacrifice legibility for tiny text. Body mostly 13–14px, labels minimum 11px; headings 24–30px. Avoid ALL CAPS except keyboard shortcuts. Fonts: deliberate system sans (-apple-system/BlinkMacSystemFont) plus SFMono/Menlo is fine and reliable. Optional remote fonts must have good fallbacks.

## Deliverable: three distinct full-product directions plus current

A quiet persistent design toolbar above the app: Mission Control / Design preview, Current / A Workbench / B Studio / C Console tabs, Sample state selector (Active, Empty, Connection lost), a presentation/focus toggle with accessible restore control. Show small 'Sample data' disclosure. Switch variants without losing selected page; default A / Terminals. Hash-route page and variant so deep links work, e.g. #a/terminals. Keyboard shortcuts must ignore focused inputs and do not override browser shortcuts except Cmd/Ctrl+K. Current tab renders parent-owned current/<page>.html in an iframe with accessible title; it is the actual original server-rendered source snapshot with client behavior disabled, initially empty. Label current honestly as 'Current UI · source snapshot / empty state'. Footer cites relevant real source file paths, quiet and compact, details disclosure okay.

All three directions show all five navigable pages. They are layout choices with different reading rhythms, not only palette swaps. Reuse page render helpers, vary meaningful shell/layout. Implement carefully without framework or dependencies.

### A Workbench — recommended, polished professional daily driver

Graphite neutral palette: background #17191c, sidebar #141618, surfaces #1d2024, borders #33363b, primary type #e9eaec, secondary #a0a5ad, selection #292d34. Muted lime #bfd38b only for primary action and running indicators. Engine accents small and restrained: Claude dusty coral, GLM blue, Codex neutral. 4–6px radii on controls, 8px on dialogs, almost no shadows. Use explicit tokens; convert to OKLCH if following Hallmark.
Left 210–224px sidebar: precise geometric MC mark, Mission Control wordmark, workspace switcher (mission-control/main), five nav links with quiet outline icons and keyboard hints; projects/session list below; bottom three engine health rows and operator. Main content grows fluidly; optional right inspector 280–310px. App topbar breadcrumb/page title left and search shortcut, contextual primary action right. Bottom tiny status strip (sample connection indicator, branch, engine usage).
Terminals landing: main pane around 65%, worker inspector around 30%. Session tab row with three realistic named sessions, active underlined, plus New; terminal thread is calm, aligned, generous. A user prompt about live model lists, a short Claude response, real filenames and 3 expandable tool rows, useful sample code block, a passing test line, composer at bottom with engine/model metadata. No giant 'Welcome' or canned marketing copy. Inspector lists two active jobs with task names, engine, elapsed, progress detail; one recent done item; selecting one opens thread/details in inspector. Relevant plan progress as a short ordered list. Add selection detail that changes, and a close to restore list.
Lanes: activity-oriented workspace. Header 'Lanes' and concise status sentence. Session rows with label/repo/current step/status/engine plus small connected horizontal Spec → Implement → Review → Verify sequence inside selected session. Below split plan and activity. Quota as compact engine rows, meaningful small meters with text; no giant KPIs/cards grid. Clicking sessions changes selected detail. 'All sessions' filter and search work with no-results feedback.
Dispatch: clear two-column work area, left job brief (title/prompt large textarea), below repo, engine/model, worktree toggle, Launch preview job; right queue and recent jobs. Validated required fields. Launch creates in-memory sample job with visible confirmation and updated queue; all copy says preview where execution could be inferred. Engine change updates model choices (include default/custom) without stale model. Show GLM off-peak status as contextual understated line.
Review: list/detail split, three sample items, file tree and usable syntax diff with + and - lines, tabs Diff/Thread, Copy review command (in-memory read-only example or clipboard if safe with catch feedback), Mark reviewed updating queue and selected state; reviewing every item reveals graceful all-clear and optional Reset samples. It must never invoke real actions.
Settings: grouped rows, Role assignments with Plan/Execute/Review each engine+model selectors, Auto-review toggle, connections and probe-preview buttons, masked settings values and preview feedback; Save changes shows state captured only for this preview. Do not expose or request real credentials. Reasonable labels, help only when it affects a choice.

### B Studio — brighter, spacious, clean engineering desk

Light cool paper #f5f5f2 / white, ink #232629, muted #616971, pale rules #d9ddde, restrained cobalt #395bc7 focus/action. No serif, terracotta, warm-clay or fashion editorial. Horizontal brand + page navigation instead of full sidebar; slim left project/session index inside relevant page; content centered max 1480px with more spacious 28–32px gutters; context appears below/right as appropriate. Same pages/details functional. Distinct composition visible immediately, not just a skin.

### C Console — compact, keyboard-first, calm Unix sensibility

Near-black neutral #101213, light gray ink, amber #dfb976 tiny highlights. Slim 56px navigation rail with real icon buttons + accessible text/tooltips, horizontally tabbed workspaces, split terminal + logs horizontally, worker/activity dock below. Monospace only for code/data, compact sans nav and titles. Distinct working layout with smaller chrome and more output on screen. No CRT glow, scanlines, ASCII decorative hero, neon theme or fake window traffic lights.

## Interactions and states

Use native dialog for Cmd/Ctrl+K command palette: search all five pages and New terminal/Dispatch actions, Arrow Up/Down and Enter, Escape, focus restore. Button near toolbar invokes it. Use native dialog for New terminal with repo, engine, matching model and session name, validation, visible in-memory tab creation; provide Resume existing sample session flow. Default terminal session selection changes prompt/content. All visible buttons/links need a real cosmetic effect, no href='#' silent stubs. Avoid drawing controls you cannot implement.
State switcher Active/Empty/Connection lost affects content across pages. Empty gives meaningful next action; lost connection shows an inline banner and Retry button visibly restoring Active sample state. Search no matches has explicit feedback and Clear. Toast/inline feedback aria-live polite; errors comprehensible. Keyboard shortcuts 1..5 can navigate while not typing; Escape closes overlays. Visible focus and accessible icon labels, semantic tables or equivalent labeled rows, non-color status text. Respect reduced motion. 320/375/414/768/1440 widths without viewport overflow; mobile sidebar reduces to navigable compact bar, multi-column content stacks or becomes accessible tabs. Text and controls never overlap.

## Work order and exit condition

Build A's app shell first, save index/styles/app early so the user sees it live; update fingerprint.js with a new unique string after each substantial rendering change to trigger browser reload. Then all five A pages/interactions, then distinct B and C layouts, then adjacent states. Keep files readable, no compressed one-line implementation. Do not write massive tests for a disposable mockup. Leave one small runnable check for navigation/fixture invariants if useful; browser verification is orchestrator-owned. While iterating run only tests for files you touch; the product full test suite is out of scope since production files are untouched. Do not self-certify visual quality without screenshot rendering; orchestrator verifies that.
Finish when three full navigable directions and Current are visible, five pages each, realistic fixtures and interactions work, no external API calls, no production edits. README lists what's simulated and links the deep links; no DECISION.md until user selects a design. Report file list and limitations in a short final response.
